# 2026-08-07

## Phantom host qualification

- ACE branch `feature/phantom-gpu-codegen` was qualified at code commit
  `ac20369c9e8a915f86c2614de644f4e06663c4c7`.
- Phantom was pinned to
  `faa6ba2bb990e17c88d826a880ef40136aaa8bc7`. The mounted checkout had
  2,068 untracked AppleDouble files, so the build used a clean `--no-local`
  clone of the pinned commit and never compiled the mounted worktree.
- The development image was built from
  `nvidia/cuda:12.4.1-devel-ubuntu22.04@sha256:5645fec64549cc35930eee9d85aafd2b0006c0c3f22632be5a1d85e2604e9749`.
  The resulting local image is
  `sha256:ce050ca193d65fee0771e51fb540868c91ef2367e60cfb47dd7c15e535a69c51`.
- The pinned environment used NVCC 12.4.131, CMake 3.22.1, GCC/G++ 11.4.0,
  NTL 11.5.1, GMP 6.2.1, C++17, CUDA 17, and architecture `sm_80`.
- The standalone real-Phantom probe compiled, performed an explicit device
  link, performed a host link, and exposed an `sm_80` cubin. Its final
  qualification manifest records Phantom archive SHA-256
  `69b8accccb6ff7d0f18dcbb63e7af7ec25792dfd5f34e75318307ea822b53ac5`
  and linked-binary SHA-256
  `b190ca9f1984d411bd2ae977a9374bb255daaf5d57c70d6148cd04294d2552bd`.
  The linked binary was not executed.

## Reproducible build boundary

- CUDA and Phantom enablement are explicit. The outer compiler propagates the
  pinned Phantom source/commit, CUDA compiler settings, `sm_80`, and language
  standards through the nested runtime and Phantom builds.
- `libFHErt_phantom.a`, `libphantom.a`, NTL, GMP, CUDA device runtime, and CUDA
  runtime are represented in the transitive link boundary. CMake 3.22 requires
  the device-runtime archive to be discovered explicitly because it does not
  provide `CUDA::cudadevrt`.
- Reached third-party sources use public repositories and immutable commits:
  GoogleTest `f8d7d77c06936315286eb55f8de22cd23c188571`, jsoncpp
  `60de77f915ab08499032d6e5a63e05e974f85d01`, ONNX
  `1089b9e8045a3a2882d7bb6a1dbaeaf9cae131da`, uthash
  `a49bed0b4abb7dff16c73906dcdc8a9718d582d2`, and BLAKE2
  `ed1974ea83433eba7b2d95c5dcd9ac33cb847913`. Generated build metadata
  contained no `code.alipay.com` reference.
- The frozen full-packed bootstrap profile SHA-256 is
  `b6f87b0f763f3984ba2d7e5ea5cbe6bce83795dd771621923c02a433306963fb`.
  The ResNet fixture manifest SHA-256 is
  `9012c8a7d84afce860e22f18a9cbd9155dcfcc923bf10101afe842f2de2291d0`.
- Immutable input checks passed for the model
  (`6627e8494884a7422fd8f2247469ae12cf8167bcabeda0033725387b8089f9d9`),
  dataset
  (`8e2eb146ae340b09e24670f29cabc6326dba54da8789dab6768acf480273f65b`),
  and preserved reference log
  (`e023426f5d371f2df34b83234adaf63644f5cc8b72d88b7b1b9b42c13ad9615f`).

## Compiler and link verification

- Final command:
  `tools/phantom_gpu/compile_only.sh --gate ckks2c`, run only inside the
  CUDA development container.
- ACE and all four native binding modules built and installed successfully.
- Focused source-generation tests: 25 passed.
- CKKS regression tests: 7 passed.
- Bootstrap source-only tests: 2 passed.
- Targeted compiler CTests: 15 of 15 passed.
- The real CKKS2C add/multiply/rotate probe generated 1,018 bytes with SHA-256
  `4b068c11a32d911ec85dfca80b56b2c8bd98430f81db890b328a532fe145e7a6`.
  It contained the Phantom header/provider and `Add_ciph`, `Mul_ciph`, and
  `Rotate_ciph`, with no forbidden provider, POLY-level, bootstrap-stage, or
  direct Phantom implementation tokens.
- The generated source passed NVCC compilation, explicit device link, host
  link against the real runtime archives, and `sm_80` cubin inspection. The
  final linked-binary SHA-256 is
  `8b715dd635a6735c51c826bb603d72f8c29ff981ab1a077db52961a5d9b1468f`.
  It was not executed.
- Final runtime archive hashes were
  `b3ef9bb43f3c670dde39ea00362f11ec8a63478deab9938f116954e86524e10c`
  for `libFHErt_phantom.a`,
  `cf695cf98cb08104201818833e36888f90acf692b7a8c6058fd1ffedf882fb32`
  for nested `libphantom.a`, and
  `376b40165e231a847f1229044804474dfc7a5e3997af9ac6a3b24c40c12b7ebe`
  for `libFHErt_common.a`.

## Container preservation

- Qualification container `30bcb4e201bd` is unprivileged, has no GPU device
  requests or NVIDIA device nodes, and remains running. `/deps/phantom-ant`,
  `/inputs/models`, and `/inputs/dataset` are read-only mounts; `/app` is the
  only writable source mount.
- The pre-existing container was preserved without deleting its writable
  state as `ace-compiler-dev-preserved-4a2bf09b9e69`; it remains running on
  its original image `sha256:51d36ed93253...`.
- Two disposable replacement containers that contained only the image entry
  command were inspected and removed after failed setup attempts. A generated
  local clone contaminated only by AppleDouble metadata was likewise inspected
  and removed; both are reproducible from pinned inputs.
- No paid resource, remote GPU, or GPU execution was used.

## Deferred runtime boundary

- The small arithmetic source/link gate is complete. Full retained-operation
  linking was not attempted because the Phantom runtime ABI for those
  operations belongs to later work.
- The production adapter is still a monolithic archive member and therefore
  contains native bootstrap symbols. The qualification records
  `production_archive_contains_native_bootstrap: true` and
  `production_archive_split_pending: true`; archive splitting remains later
  work rather than a host-qualification failure.
- No encoding, ownership, level/scale, runtime correctness, or GPU execution
  behavior was implemented or claimed.

## Local commits

- `ad06e29` `build(phantom): pin CUDA runtime toolchain`
- `798b1bc` `tools(phantom): add SM80 compile-only qualification`
- `ac20369` `test(ckks): use functional codegen fixture names`

The pre-existing modified and untracked user files were not staged or changed
as part of these commits.

## Qualification hardening and registry handoff

- The hardened qualification code is commit
  `c02f39c3dde8a4d5f923499133b590b942c8e212`. The final CKKS2C evidence run is
  `20260807T103723Z-2048.eGKJva`; its tracked-source manifest matches the
  commit even though unrelated user files keep the overall worktree dirty.
- The final host gate passed 49 focused codegen/tool tests, 7 CKKS regression
  tests, 2 bootstrap source-only tests, and 15 of 15 selected CTests. The
  positive native terminal route selected CKKS2C and exited zero; the negative
  POLY/Phantom route exited one with no source output.
- The generated add/multiply/rotate source is 1,019 bytes with SHA-256
  `965f10c27b4cb2a5935acfa2b4898036686f319d1972b78cf0d3e9346935f91c`.
  It contains the Phantom provider/header and required arithmetic calls, and
  the source audit found no POLY, ANT, SEAL, bootstrap, stage, or profile
  mismatch. NVCC compilation, explicit device link, host link, and `sm_80`
  cubin inspection all passed without executing the binary locally.
- The qualified development image config ID is
  `sha256:58d0e405d3806d9aec988b734fc8869e4335e1fd0ee5c5d1d0f539a40b2961f7`;
  its definition SHA-256 is
  `f3638149315721b64f4c2584eaef481c85897baf88389c308076bb0b9629e69e`.
  The image contains no repository source, model/dataset payload, credentials,
  or baked SSH host keys.
- The source-free image was published to the unique one-hour tag
  `ttl.sh/ace-phantom-health-20260807-c02f39c-58d0e405:1h`. Its immutable
  registry manifest digest is
  `sha256:98c16a51585df740f73d00601dd5b28ba6c503357295f03ff0464057ed758a53`,
  whose config digest exactly matches the qualified local image ID.
- The checksum-bound A100 bundle archive is
  `build/phantom_gpu/a100_health_bundles/20260807T103723Z-2048.eGKJva.tar.gz`
  with SHA-256
  `ecc3df0cc4559076211ecf1d1353a322df9fb22c33771ce5b91cb36774034153`.
  Its health binary SHA-256 is
  `99e896ab1bdd2bf7a3f34e84a4460f51c27f5148c9bd76b462a44ef534d4b44e`,
  and its shared-profile SHA-256 is
  `b6f87b0f763f3984ba2d7e5ea5cbe6bce83795dd771621923c02a433306963fb`.
  The archive sidecar and every internal checksum passed before launch.

## RunPod launch result

- The first launch attempt returned HTTP 402 before allocating a pod. Its
  private template was deleted, and the account was funded before another
  launch was attempted.
- A subsequent pod, `zktw9gf01qb6qu`, was created at
  `2026-08-07T11:43:13.440Z` but stopped before bundle transfer while explicit
  source-free egress authorization was obtained. Stop returned at
  `11:45:07.302Z`; the pod was deleted at `11:45:12.739Z` and template
  `gtwomp1iyd` at `11:45:15.796Z`. Its rate-based cost estimate was about USD
  0.044, and cleanup left zero pods and private templates.
- The authorized health run used Secure Cloud pod `ru7msji94b88n3` in
  `EU-RO-1`, one exact `NVIDIA A100 80GB PCIe`, and the immutable qualified
  image above at USD 1.39/hour. RunPod recorded creation at
  `2026-08-07T11:48:10.565Z` and start at `2026-08-07T11:48:10.559Z`.
- The source-free bundle was uploaded separately. Its remote SHA-256 and every
  internal checksum matched before extraction. The first supervisor invocation
  exited before GPU discovery because an SSH login shell did not inherit the
  container identity environment; its failure archive was retrieved with
  SHA-256
  `e09fb8c3d70c8d23879d8cd21db7ab2a728876b6842fa13db47318d45923dc5e`.
- A single non-automatic retry exported the already verified identity values
  recorded in PID 1. From `2026-08-07T11:51:24Z` through `11:51:25Z`, the
  runner saw exactly one `NVIDIA A100 80GB PCIe`; the linked native health
  executable exited zero and reported maximum error `1.82039e-14`. Its image,
  definition, registry, profile, and health-binary identities exactly matched
  the qualified bundle.
- The successful result archive was checksummed remotely, retrieved, and
  verified locally at
  `build/phantom_gpu/a100_health_results/20260807T103723Z-2048.eGKJva-authorized/result.tar.gz`.
  Its SHA-256 is
  `56fc23f85fafa25d1bfdc145a7ad101da9ef9bc615ffef46b32eb92744fe82fc`.
  The extracted `state.json` and `result.json` both report pass, health exit
  zero, and one exact A100; native stderr is empty.
- The pod stop response reported `EXITED` after 216 seconds of uptime. The pod
  and private template `b54tpzd6u7` were each deleted with HTTP 204. A final
  check at `2026-08-07T11:58:41.104Z` found zero pods and zero private
  templates. The rate-based GPU-cost upper bound is USD 0.0834; RunPod's
  time-bucketed billing API had not yet posted the short run and still reported
  zero.
- External lifecycle evidence is preserved beside the result archive in
  `orchestration.json`. The API responses did not expose exact stop/delete
  response timestamps, so the recorded stop time is explicitly derived from
  start time plus integer uptime rather than presented as a provider timestamp.
- Evidence portability fix `7577dbf` publishes bundle archives and sidecars as
  mode `0644`, makes returned result archives independent of the caller umask,
  cleans failed archive temporaries, and adds restrictive-umask integration
  tests. Post-fix verification passed 51 focused tests, 7 regressions, 2
  bootstrap source-only tests, and 15 of 15 selected CTests.
- This closes only the linked native Phantom-ant A100 health gate. It does not
  claim retained-operation correctness, generated bootstrap execution,
  sanitizer, benchmark, or ResNet20 coverage.

## Lessons from the hardened handoff

- Atomic evidence written through `mktemp` must be changed to mode `0644`
  before rename so host-side checksum verification remains possible after a
  root-owned container run.
- Bundle packaging must validate the current-record pointer, immutable run
  manifest, complete `SHA256SUMS` chain, qualified binary hash, and shared
  profile hash before rebinding them to a registry reference.
- A registry manifest digest and a local image/config digest are different
  identities. Both must be recorded, and the remote manifest's config digest
  must match the qualified local image ID.
- SSH host keys must be generated when the ephemeral pod starts, never baked
  into a reusable or publicly reachable image. A registry expiry is not cloud
  cleanup; exact-ID stop/delete and a final absence listing remain mandatory.
- Archives created through a pre-existing `mktemp` file inherit its private
  mode even when `tar` rewrites the contents. Set the mode before publication,
  make checksum sidecars explicit, and test both outbound and returned archives
  under a restrictive umask.
- RunPod's container environment is visible to PID 1 but is not necessarily
  inherited by an SSH login shell. The supervisor must receive the pinned
  identity values explicitly while still comparing them with the bundle
  manifest.
- Replaying a content-hashed bootstrap on a pinned CUDA base image can avoid a
  large custom-image upload, but it does not satisfy the present same-image
  contract without an explicit plan change and fresh host qualification.

## Pinned runtime-bootstrap pipeline

- Protected Docker container `ace-compiler-dev` remained running with container
  ID `ebb8bf9a6c43d3903893dfc436ba7db156adf6b4b4037933e83540394c3eb05c`
  and image ID
  `sha256:58d0e405d3806d9aec988b734fc8869e4335e1fd0ee5c5d1d0f539a40b2961f7`.
  Its mounts and writable state were not changed.
- Exact qualification-only containers
  `2d568939ddb2b8fdc580536b8a707e617271db321854c4f475eff6003d5a953f`
  and `30bcb4e201bd543e33ceca6231dc20621a249befadb13540cb1cf580a2cd735c`,
  and their exclusive images
  `sha256:024d8c83d35a1fad669c8b2c6d3b5197ba895d0e6a96a0e557cd8855a9c74109`
  and `sha256:ce050ca193d65fee0771e51fb540868c91ef2367e60cfb47dd7c15e535a69c51`,
  were removed without volumes or a global prune. Docker image usage fell from
  60.99 GB to 58.87 GB. Ambiguous preserved artifacts were left untouched.
- The replacement development workflow uses official NVIDIA image
  `docker.io/nvidia/cuda:12.4.1-devel-ubuntu22.04@sha256:5645fec64549cc35930eee9d85aafd2b0006c0c3f22632be5a1d85e2604e9749`;
  its linux/amd64 config digest is
  `sha256:0131784115794405cb36a8068a82d7aea0937196d1d6e844b9dd021252ccf7e4`.
  Exact direct APT candidates, hashed Python wheels, base-file fingerprints,
  source commits, build options, and the complete resolved package manifests
  are recorded by the pipeline. Ubuntu repository snapshots remain the stated
  reproducibility limitation for transitive APT packages.
- Packaging and local reproduction now require explicit full
  `--ace-commit` and `--phantom-commit` arguments. The requested Phantom object
  must equal the dependency lock read from the selected ACE commit. Payload
  scripts and source archives also come from those exact commit objects rather
  than the current checkout.
- The final audited payload uses ACE
  `196c8b9771b92a7dda1c277ba8dc2e62b90b9518` and Phantom
  `faa6ba2bb990e17c88d826a880ef40136aaa8bc7`. Its ACE archive SHA-256 is
  `1b11f1564524b158df4bec6af9e50dec8c8eed7cc4935bd2b010611ce82139c7`;
  its Phantom archive SHA-256 is
  `fff838fcdbcebaf78865f14ccd3e91b4bad18792e2a254d3c7ad5c527f3259d6`.
  Two independently packaged directories were byte-identical.
- The final cold local run passed in 396 seconds: environment bootstrap 186,
  source audit/extraction 0, and qualification 210. The qualification included
  56 focused Python tests, 7 regressions, 2 bootstrap source tests, 15 selected
  CTests, terminal-selection positive/negative checks, and `sm_80` NVCC compile,
  device-link, host-link, and cubin inspection. Its result archive SHA-256 is
  `ebfdaa21a30439a007bb8e88995ce11be1849ecc1b2f67bbca4be076e0666ce9`.
- A diagnostic run exposed that `set +e` around a shell function let commands
  after an internal failure continue and could turn a failed gate into success.
  Timed phases now execute in a fail-fast subshell, preserve their duration and
  exit status, and have a deliberate failure regression test. Result checksum
  sidecars now contain portable basenames.
- Immediately before the intended paid run, RunPod listed zero pods, private
  templates, network volumes, and registry credentials. Secure A100 PCIe list
  pricing was USD 1.39/hour, but two explicit CUDA 12.4 capacity probes returned
  `Out` for `NVIDIA A100 80GB PCIe`. No template, pod, SSH key, source transfer,
  or charge was created. The exact GPU is fixed by the plan, so remote pipeline
  validation and the operational plan update remain pending capacity.
