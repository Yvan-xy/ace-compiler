## Work done

- Read the repo-local `agents.md` and followed its constraints.
- Investigated the non-DSL bootstrap test path for `resnet20_cifar10` using:
  - `fhe-cmplr/test/cgo25_ae.py`
  - `fhe-cmplr/test/ace_compile.py`
  - `fhe-cmplr/test/run_perf.py`
  - `fhe-cmplr/test/run_accuracy.py`
  - `fhe-cmplr/test/ace_util.py`
  - `fhe-cmplr/test/test_bts_rescale.sh`
  - `fhe-cmplr/test/docker/Dockerfile`
- Verified the current local machine does not match the expected `/app` container layout.
- Verified the current checkout does not contain:
  - installed compiler trees such as `ace_cmplr` / `ace_cmplr_omp`
  - built artifacts such as `fhe_cmplr`, `libFHErt_ant.a`, `libFHErt_common.a`, `libAIRutil.a`
  - ONNX model files such as `resnet20_cifar10_pre.onnx`
  - CIFAR dataset files such as `cifar-10-batches-bin/test_batch.bin`
- Verified the current Python environment is missing `psutil`, which blocks `cgo25_ae.py` at import time.
- Verified the host toolchain is macOS/Apple clang and `/usr/bin/time` does not support GNU `-f`, which the AE scripts require.
- Added a repo-level `Dockerfile` based on `ubuntu:latest` to provide a Linux-compatible development/test environment with the missing system and Python dependencies, including `psutil`.
- Standardized the container dataset mount point at `/dataset`, with convenience links `/app/dataset`, `/opt/datasets/cifar-10-batches-bin`, and `/opt/datasets/cifar-100-binary`.
- Built the compiler successfully inside the persistent Docker container `ace-compiler-dev`.
- Built from `/app` using `cmake -S fhe-cmplr -B build -DFHE_WITH_SRC="air-infra;nn-addon" -DBUILD_WITH_OPENMP=ON` with tests/bench/examples/code-check disabled and install prefix left at the default `/usr/local`.
- Verified key artifacts:
  - `/app/build/driver/fhe_cmplr`
  - `/usr/local/bin/fhe_cmplr`
  - `/usr/local/lib/libAIRutil.a`
  - `/usr/local/lib/libFHEckks.a`
  - `/usr/local/rtlib/lib/libFHErt_ant.a`
  - `/usr/local/rtlib/lib/libFHErt_common.a`
- Worked around private dependency fetches without editing source by:
  - using `FETCHCONTENT_SOURCE_DIR_ONNX=/opt/venv/lib/python3.12/site-packages`
  - using `FETCHCONTENT_SOURCE_DIR_JSONCPP=/app/third_party/jsoncpp`
  - setting container-local git `insteadOf` rewrites for private `jsoncpp`, `uthash`, `BLAKE2`, `googletest`, and `benchmark` URLs
- Rebuilt the compiler in `/app/build` with unit tests and benchmarks enabled:
  - `BUILD_UNITTEST=ON`
  - `BUILD_BENCH=ON`
  - `FHE_BUILD_TEST=ON`
  - `NN_BUILD_TEST=ON`
  - `AIR_BUILD_TEST=ON`
  - code checks disabled for `AIR`, `NN`, `FHE`, and `RTLIB`
- Verified `ctest -N` in `/app/build` reports 76 registered tests.
- Fixed two real rtlib unittest compile errors uncovered by enabling tests:
  - added `#include <iomanip>` to `fhe-cmplr/rtlib/ant/unittest/ut_ckks_perf.cxx`
  - added `#include <iomanip>` to `fhe-cmplr/rtlib/ant/unittest/ut_ksw_opt.cxx`
- Connected to the server `yifan@10.28.27.58` and worked in `/media/newhd/yifan/ace-compiler`.
- Prepared `/media/newhd/yifan/ace-compiler/third_party` with public clones of:
  - `jsoncpp`
  - `googletest`
  - `benchmark`
  - `uthash`
  - `BLAKE2`
- Built the server Docker image from the repo `Dockerfile`.
- Verified resulting server image tag: `ace-compiler-dev:latest` (image id `47b33423f153` at build time).
- Built the older server checkout inside the running container `ace-compiler-dev` under `/app/build` with Ninja, default install prefix, and unit tests enabled.
- Server-side container build flags that succeeded:
  - `-G Ninja`
  - `-DFHE_WITH_SRC="air-infra;nn-addon"`
  - `-DBUILD_WITH_OPENMP=ON`
  - `-DBUILD_UNITTEST=ON`
  - `-DBUILD_BENCH=OFF`
  - `-DFHE_BUILD_TEST=ON`
  - `-DNN_BUILD_TEST=ON`
  - `-DAIR_BUILD_TEST=ON`
  - code checks disabled for `AIR`, `NN`, and `FHE`
- Additional server-only fixes/workarounds needed for the older checkout:
  - installed `pybind11` in the container and passed `pybind11_DIR=/usr/local/lib/python3.8/dist-packages/pybind11/share/cmake/pybind11`
  - injected `-I/usr/local/lib/python3.8/dist-packages/pybind11/include` via `CMAKE_CXX_FLAGS` because the older `air-infra/plugin/CMakeLists.txt` does not add pybind11 include paths
  - installed `nlohmann-json3-dev` inside the running container
  - copied the local `nn-addon/onnx2air/src/onnx2air.cxx` fix to the server checkout so ONNX parsing uses `ParseFromString`
  - copied the local `<iomanip>` fixes for `fhe-cmplr/rtlib/ant/unittest/ut_ckks_perf.cxx` and `ut_ksw_opt.cxx` to the server checkout
- Verified server container result:
  - `/app/build/driver/fhe_cmplr`
  - `/usr/local/bin/fhe_cmplr`
  - `ctest -N` in `/app/build` reports 76 tests
- Updated the newer local repo `Dockerfile` so it explicitly installs `nlohmann-json3-dev`; `pybind11-dev` and pip `pybind11` were already present there.

## Takeaways

- The user's usual command is container-oriented. `/app` in that command corresponds to the repo root inside the test Docker image.
- `cgo25_ae.py` is not just a compile test:
  - with `-p`, it disables performance mode
  - it still runs the accuracy path by default
  - it expects an OpenMP-enabled installed compiler tree from `-co`
  - it needs CIFAR input data to run the produced executable
- For a native non-DSL bootstrap regression, `fhe-cmplr/test/test_bts_rescale.sh` is the closer backend-only test. It compiles `resnet20_cifar10.onnx` with `-CKKS:mxbl=16:mbc=2:sbm:tsbp` and diffs the resulting bootstrap level log.

## Lessons learned

- When bringing up backend tests first, distinguish three paths early:
  - compile-only regression (`test_bts_rescale.sh`)
  - compile+run AE (`cgo25_ae.py`)
  - EDSL/example-level tests (`ace_edsl/tests/...`)
- On this machine, the blockers are layered:
  1. missing Python dependency (`psutil`)
  2. missing test inputs (ONNX and CIFAR data)
  3. missing installed/build compiler artifacts
  4. host mismatch with Linux/GNU assumptions in the test harness
- The repo does contain generated `.onnx.inc` placeholders under `fhe-cmplr/rtlib/ant/dataset/`, but not the original `.onnx` model files needed by the native compiler test scripts.
- For repeatable backend bring-up here, the cleanest path is to use the Docker image rather than patch the Linux-oriented AE scripts for macOS first.
- The default install prefix build in the container is now viable, so `cgo25_ae.py` can point to `-co /usr/local` as originally intended.
- `make test` at repo root is still wrong for this repo; the working test entrypoint for the configured tree is `ctest` from `/app/build`.
- The server checkout appears to be an older repo shape than the laptop checkout: it has `air-infra`, `nn-addon`, `fhe-cmplr`, `model`, `scripts`, and a `Dockerfile`, but not the newer `ace_edsl`/`acepy` layout.
- The older server checkout is not source-compatible with the newer laptop checkout in two places:
  - `nn-addon/onnx2air/src/onnx2air.cxx` still expects `ParseFromIstream`
  - `air-infra/plugin/CMakeLists.txt` does not add pybind11 include directories
