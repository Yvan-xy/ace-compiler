# Vector-kernel DSL lowering M7

## Work done

- Closed the validation gate with test-only changes; no production source,
  bindings, CKKS path, or tentative inliner changed.
- Added baseline Conv provider/provenance-independent identity coverage.
- Covered missing providers and missing DSL recipes for forced baseline Gemm
  and Conv under both error and diagnosed C++/native fallback policies.
- Proved all post-materialization recipe failures remain fatal with fallback
  enabled and asserted the exact valid pre-inline stopping stage.

## Tests

All builds and tests ran in `ace-compiler-dev`.

- Focused baseline tests: 15/15 Python tests and 3/3 new C++ tests passed.
- Complete `ut_nnvector`, including shuffle seed 4242: 72/72 passed.
- Complete repository build passed.
- The EDSL quick harness passed 97 unit tests and all applicable local
  integrations. The full harness additionally reported its ONNX integration
  unavailable because `/app/model/resnet20_cifar10_pre.onnx` is absent.
- The complete `ace_edsl/tests` collection had 125 passing tests and three
  pre-existing CKKS scale-manager assertion failures.
- Complete repository CTest: 74/76 passed. `test_fhe_cmplr` and
  `test_bts_rescale` lack `ossutil64` and their external ONNX/SIHE inputs.
- Baseline Gemm and Conv native/DSL end-to-end comparisons passed.

## Follow-on

- M7 is complete. M8 and deferred provider/inliner work remain untouched.
